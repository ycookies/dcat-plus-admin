# 开发任务看板（{pluginName}）

> 勾选规则：完成一项把 `☐` 改为 `☑`。本文件随开发进展持续更新，是「现在做到哪一步」的唯一事实来源。

## 阶段 0 · 脚手架就绪

- [x] 扩展骨架已生成（Service Provider / 路由 / 控制器 / 资源）
- [x] version.php 已创建{apiOrNone}

## 阶段 1 · 数据层完善

> 核对 [database/schema.md](../database/schema.md) 中已生成的表；占位表需补充字段。
> 当前数据表：{tables}
> **表名前缀**：`{tablePrefix}`（新建表必须加，反推表保持原名，详见 schema.md「表名前缀约定」）

- [ ] 核对每个表字段类型 / 长度 / 注释与业务一致
- [ ] 为占位表（若反推时表不存在）补充真实字段，注释加 `@scaffold:type` / `@scaffold:options`
- [ ] 确认新建表名都带前缀 `{tablePrefix}`，Model 的 `$table` 与物理表名一致
- [ ] 补充表注释（`COMMENT='业务实体'`）
- [ ] 建立关联（`user_id` 等外键列）与索引
- [ ] 执行 `php artisan admin:ext-update {extensionName}` 让迁移生效
- [ ] 运行 seeder 确认幂等（带 `--seed` 时已生成）

## 阶段 2 · 后台 CRUD

> **铁律：用命令行脚手架生成，不要手写 Controller 骨架。**
> 按 [schema.md](../database/schema.md) 逐表执行：
> `php artisan admin:scaffold --table=<表名> --extension={extensionName} --force`
> 命令自动生成 Model / Controller / Lang / 后台路由 / 菜单 / 权限。再按 [database-schema.md](../../../../.claude/skills/dcat-plus-admin/references/database-schema.md) 补充人工项。

- [ ] 用 `admin:scaffold --extension={extensionName}` 生成各表的 Controller（字段注释写得好，控件就准）
- [ ] 补充关联下拉、上传 disk/dir、富文本等人工项
- [ ] Grid 筛选器 / 快捷搜索 / 列显示器调优

## 阶段 3 · 业务逻辑

- [ ] 表单 saving/saved 事件钩子
- [ ] Repository / 模型作用域
- [ ] 自定义 Action（见 references/action-system.md）
- [ ] 中间件（如需）

## 阶段 4 · 接口开发

> **铁律：用命令行脚手架生成 API 控制器与 Resource，不要手写骨架。**
> `php artisan admin:scaffold --table=<表名> --extension={extensionName} --api --admin-api --force`
> 生成 member-api / admin-api 控制器（继承 BaseApiController）+ Resource 类，路由自动注册。
> 维护 [delivery/api_delivery.md](../delivery/api_delivery.md) 作为移动端唯一数据源。

- [ ] 用 `admin:scaffold --api/--admin-api --extension={extensionName}` 生成 API 控制器 + Resource
- [ ] 补全各控制器的 `getValidationRules()`（脚手架留空实现）
- [ ] 按业务裁剪默认生成的列表/批量/导入导出接口
- [ ] Scramble 注释（方法 PHPDoc / validate 字段注释 / Resource 注解）
- [ ] 汇总交付文档 [api_delivery.md](../delivery/api_delivery.md)

## 阶段 4.1 · 前端小程序页面开发（带 --api 时）

> **铁律：前端 AI 只读 [api_delivery.md](../delivery/api_delivery.md) 对接接口，不读后端控制器、不猜接口。** 契约有出入先回阶段 4 修文档。
> 基于 `web_tpl/association`（unibest：uni-app + Vue3 + TS + wot-ui + pnpm）开发。先把 `association` 复制为独立前端工程再 `pnpm install`。

- [ ] 通读 `api_delivery.md`，按「用户流程」列出页面清单（流程步骤 → 页面 → 所需接口）
- [ ] 配置 `.env` 的 `VITE_SERVER_BASEURL` 指向 member-api 地址（含统一前缀，避免与接口路径重复）
- [ ] 建立请求层：有 openapi.json 跑 `pnpm openapi` 生成 `src/service/`；否则按接口清单写 `src/api/<模块>.ts` + 类型
- [ ] 按用户流程逐页开发（`src/pages/<模块>/`，wot-ui 渲染，列表页接 `{list, page_info}` 分页）
- [ ] 接入鉴权（登录存 token 到 `useTokenStore`，受保护页靠拦截器自动带 token）
- [ ] `pnpm dev:h5` 或 `dev:mp` 跑通至少一条核心主流程（登录→列表→详情→下单→我的）

## 阶段 4.2 · 落地页开发（带 --marketplace 时）

> **创作题不是填空题**：空壳 `resources/views/landing.blade.php` 只提供统一技术基线（独立 HTML + Tailwind v4 CDN + Lucide + CSS 变量换肤），body 为空。按本扩展**真实业务功能**自主设计结构与内容，不套固定模板。
> 质量要求五条：美观（品牌色）、内容丰富（对应真实模块）、层次感（区块节奏）、动态感（渐入/计数/hover + reduced-motion 降级）、自带图片（AI 生图，无占位）。
> 参考标杆：`dybang.saishiyun.net/resources/views/fishing_web.blade.php`（学水准，不照抄结构）。

- [ ] 读 BLUEPRINT.md / schema.md 提炼业务价值，定下落地页要讲的区块（对应真实功能模块，不凑数）
- [ ] 品牌色写入 `:root` 变量，独立路由可访问，不继承后台布局
- [ ] 实现五项目标效果（美观/丰富/层次/动态/图片齐全）
- [ ] 所需图片全部 AI 生图填充到 `public/images/{extensionName}/`，无占位灰块
- [ ] 响应式（桌面/平板/移动端）+ `prefers-reduced-motion` 无障碍降级
- [ ] 文案对应真实功能、行业语言，无通用废话；统计数据标 TODO 待换真实值

## 阶段 5 · 发布准备

- [ ] 完善 README（若 `--marketplace` 则完善 MARKETPLACE.md）
- [ ] 核对落地页（阶段 4.2 已完成）最终可访问、图片齐全
- [ ] composer.json 元信息（name/description/authors）
- [ ] 打 tag 并发布到应用市场

---

## 当前进度

- **当前阶段**：阶段 1 · 数据层完善
- **上一步完成**：阶段 0 脚手架生成
- **下一步**：核对 / 补充表结构字段

## 交付检查清单（发布前）

- [ ] 所有表已在 version.php 登记，`ext-update` 能正确执行
- [ ] 后台 CRUD 全部可访问且权限正确
- [ ] 接口交付文档与实际接口一致
- [ ] 市场介绍 / 落地页就绪
- [ ] README 含安装与使用说明
