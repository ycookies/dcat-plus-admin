# 接口交付文档（{pluginName}）

> 本文件是扩展 `{extensionName}` 的**接口交付文档**，读者是接手移动端（小程序 / H5 / App）开发的 AI。
> 目标：**只阅读本文件**即可理解业务场景、页面数据依赖、接口契约与枚举含义，无需阅读 Controller / Model / 迁移文件。
> 接口或字段变更时，必须在同一任务内更新本文件。规范见 [api-development.md](../../../../.claude/skills/dcat-plus-admin/references/api-development.md)。

## 一、业务上下文（context）

### 模块概览
- **模块名称**：{pluginName}
- **扩展包**：`{extensionName}`
- **业务目标**：_待补充（解决什么问题、面向什么角色）_

### 核心实体
> 数据表见 [../database/schema.md](../database/schema.md)。当前已生成表：{tables}

| 实体 | 主键 | 关键字段 | 与其他实体的关系 | 移动端用途 |
| --- | --- | --- | --- | --- |
| _示例实体_ | `id` | `name`、`status` | — | 列表 / 详情 |

### 用户流程
1. _待补充：用户进入 → 查看 → 操作 → 结果反馈的完整路径。_

### 页面与数据依赖
| 页面 / 组件 | 调用接口 | 必需数据 | 加载时机 | 空态 / 异常处理 |
| --- | --- | --- | --- | --- |
| _列表页_ | `GET /member-api/...` | _字段_ | 页面进入 / 下拉刷新 | 空列表占位 |

### 鉴权与业务边界
- **会员端（member-api）**：需登录接口携带 `Authorization: Bearer <token>`，登录走 `POST /member-api/...`。
- **管理端（admin-api）**：后台管理员使用，C 端一般不调用。
- _待补充：哪些接口免登录、有哪些幂等 / 重复提交 / 状态前置约束。_

---

## 二、通用约定（务必遵守）

- **会员端 Base URL**：`/member-api`
- **管理端 Base URL**：`/admin-api`
- **鉴权**：需登录接口请求头携带 `Authorization: Bearer <token>`。
- **成功响应**：`code` 为 `0`、`status` 为 `success`。
- **失败响应**：以 `code`、`msg` 为准；**前端不得只依据 HTTP 状态码判断业务结果**。
- **分页响应**：读取 `data.list` 与 `data.page_info`，**不要假定接口返回全量数据**。
- **分页参数**：Query 传 `pageSize`（默认 10）；排序传 `sort`（字段名）+ `order`（`asc`/`desc`）。
- **过滤**：基类 `lists()` 仅对 fillable 字段做**精确等值**；模糊 / 范围查询需专用接口（勿用 `[like]` 语法）。
- **敏感数据**：示例中的密码 / token / 手机号 / 身份证必须脱敏或虚构，禁止真实数据。

### 响应结构示例
```json
{
  "code": 0,
  "status": "success",
  "msg": "ok",
  "data": {
    "list": [],
    "page_info": { "pageSize": 10, "page": 1, "total": 0 }
  }
}
```

---

## 三、接口清单（api_list）

> 每个接口记录：用途、方法与路径、鉴权、路径/Query/Body 参数、成功响应 JSON、字段表、失败场景。

### _接口名_（示例，待替换）
- **用途**：_接口的业务作用_
- **请求**：`GET /member-api/...`
- **鉴权**：否 / 是（member-api 登录态）

#### Query 参数
| 字段 | 类型 | 必填 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `pageSize` | integer | 否 | `10` | 每页条数 |
| `sort` | string | 否 | `id` | 排序字段 |
| `order` | string | 否 | `desc` | `asc` / `desc` |

#### 成功响应示例
```json
{
  "code": 0,
  "status": "success",
  "msg": "ok",
  "data": {
    "list": [
      { "id": 1, "name": "示例", "status": 1 }
    ],
    "page_info": { "pageSize": 10, "page": 1, "total": 1 }
  }
}
```

#### 字段说明
| 字段路径 | 类型 | 可空 | 含义 | 移动端处理建议 |
| --- | --- | --- | --- | --- |
| `data.list[].id` | integer | 否 | 主键 | 用于详情 / 后续请求 |
| `data.list[].status` | integer | 否 | 状态 | 见枚举字典 |

#### 失败场景
| `code` / 场景 | `msg` 示例 | 前端处理 |
| --- | --- | --- |
| `401` 未登录 | `未登录` | 清理登录态并进入登录流程 |
| `422` 参数校验失败 | `字段必填` | 保留页面并停止加载 |

---

## 四、枚举字典（enum）

> 所有前端可见的状态 / 类型 / 角色枚举集中维护于此。新增 `@scaffold:options` 字段时同步补到本节。

### _枚举名_（示例）
- **作用字段**：`table.status`、`data.list[].status`

| 值 | 文案 | 业务含义 | 移动端展示 / 可执行操作 |
| --- | --- | --- | --- |
| `0` | 禁用 | _含义_ | _展示与操作_ |
| `1` | 启用 | _含义_ | _展示与操作_ |

---

## 维护要求（摘自 api-development.md）

- 每完成一个可供移动端使用的业务模块接口，必须同步更新本文件。
- Scramble PHPDoc 与本文件保持一致；冲突时先修正代码契约，再同步文档。
- 字段表必须用完整路径（如 `data.list[].status`），标明类型、可空、含义、处理建议。
- 时间格式、金额单位、图片 URL 是否完整可访问必须明确。
