{{--
    {pluginName} 应用市场落地页（空壳）

    本文件是落地页的统一起点：所有扩展包的落地页都用这个独立 HTML 结构，方便统一管理。
    具体内容（区块、文案、色彩、图片、动效）由 AI 或开发者按业务功能自由生成，
    不预设固定结构，不要照搬其他扩展。质量要求见 extension-workflow.md「阶段 4.2 · 落地页开发」。

    技术基线（已就绪，保持不变）：
    - 独立 HTML，不继承后台布局，面向访客，可独立路由访问。
    - Tailwind v4 浏览器版 CDN + Lucide 图标 CDN（无需 npm 构建，部署即用）。
    - 设计令牌：用 :root CSS 变量定义品牌色，方便换肤。

    生成器已替换：{pluginName}（系统名）、{pluginDesc}（一句话定位）、{extensionName}（包名）。
    下方 <body> 为空，请在里面按业务生成完整的落地页内容。
--}}
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{pluginName} - {pluginDesc}</title>
    <meta name="description" content="{pluginDesc}">

    {{-- 设计令牌：换成 {pluginName} 的品牌色 --}}
    <style>
:root {
  --brand-primary: #4F46E5;          /* 主色：按业务改 */
  --brand-accent: #F59E0B;           /* 强调色：按业务改 */
  --brand-bg: #FFFFFF;
  --brand-fg: #14142B;
}
    </style>

    {{-- Tailwind v4 浏览器版（自包含，无需构建） --}}
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4.3.1/dist/index.global.js"></script>
    {{-- Lucide 图标（按需用 <i data-lucide="图标名"></i>，文末调 lucide.createIcons()） --}}
    <script src="https://unpkg.com/lucide@1.8.0/dist/umd/lucide.min.js"></script>
</head>
<body>
    {{-- TODO: 在此按 {pluginName} 的业务功能生成落地页内容。
         要求（详见 extension-workflow.md 阶段 4.2）：美观、内容丰富、有层次感、有动态感、自带所需图片。 --}}



    {{-- 初始化 Lucide 图标（若使用了 data-lucide 图标则必须调用） --}}
    <script>lucide.createIcons();</script>
</body>
</html>
